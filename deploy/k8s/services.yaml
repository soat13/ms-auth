apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth-service
  name: auth-service
  namespace: fiap
spec:
  type: NodePort
  ports:
  - name: 30008-8090
    port: 30008
    targetPort: 8090
    nodePort: 30008
  selector:
    app: oficina-auth