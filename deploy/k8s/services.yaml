apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth-service
  name: auth-service
  namespace: fiap
spec:
  type: NodePort
  ports:
  - name: 3000-8090
    port: 3000
    targetPort: 8090
    nodePort: 30009
  selector:
    app: oficina-auth